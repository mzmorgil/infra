name: Deploy GKE Cluster with OpenTofu

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    timeout-minutes: 10  # Job-level timeout as a fallback

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup OpenTofu
        uses: opentofu/setup-opentofu@v1
        with:
          tofu_version: 1.9.0

      - name: Authenticate to GCP
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: "projects/664095006793/locations/global/workloadIdentityPools/github-actions-pool/providers/github-provider"
          service_account: "mzm-cicd-sa@mzm-org-il.iam.gserviceaccount.com"

      - name: Verify GCP Auth
        run: gcloud auth list

      - name: Verify GCS Bucket Access
        run: gsutil ls gs://mzm-org-il-terraform-state

      - name: OpenTofu Init
        run: tofu init -backend-config="bucket=mzm-org-il-terraform-state" -backend-config="prefix=gke-cluster/state"

      - name: OpenTofu Plan
        env:
          TF_LOG: DEBUG          # Set log level to DEBUG
          TF_LOG_PATH: plan.log  # Write logs to plan.log
        run: |
          # Run tofu plan with a 2-minute timeout
          timeout 120s tofu plan -out=tfplan || true
          # Display the log file, even if timeout occurs
          if [ -f plan.log ]; then
            cat plan.log
          else
            echo "No logs captured."
          fi

      - name: OpenTofu Apply
        run: tofu apply tfplan